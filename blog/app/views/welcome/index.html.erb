<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, intiial-scale=1.0">
    <title>Study Buddy | UNC Charlotte</title>
    <link href="https://fonts.googleapis.com/css?family=Carter+One" rel="stylesheet">
</head>

<body>
  
    <!-- HEADER for Clearence bundle-->
    <header>
        <% if signed_in? %>
          Signed in as: <%= current_user.email %>
        <%= button_to 'Sign out', sign_out_path, method: :delete %>
        <% else %>
          <%= link_to 'Sign in', sign_in_path %>
        <% end %>
    </header>
    
    <!-- For Clearence bundle -->
    <section>
        <div id="flash">
          <% flash.each do |key, value| %>
        <div class="flash <%= key %>"><%= value %></div>
        <% end %>
    </div>
    </section>
    
    <main>
         <!-- BODY -->
        <!-- CSS grid for creating structure of the body on the website-->
        <div class="wrapper">

            <div class = "header">
            </div>
            
            <div class="imp_text">
                <h1>Study Buddy</h1>
            </div>
            
            <div class="text">
                <p class="sub_imp_text">Find your buddy for study </p>
                <br>
                <p class="info">Use this wonderful tool to find your partner for preparing for your classes at UNC Charlotte</p>
                <br>
            </div>
            
            <!-- HEADER for Clearence bundle-->
    <div class="btn">
        <% if signed_in? == false %>
          <%= button_to 'Sign in', sign_in_path, :id => "big_button", :method => :get %>
        <% end %>
    </div>
    
    <div class="btn">
        <% if signed_in? %>
          Signed in as: <%= current_user.email %>
       <%= button_to "Click Here to Start", buddies_path, :id => "big_button", :method => :get %>
        <% else %>
          <%= button_to 'Sign up', sign_up_path, :id => "big_button", :method => :get %>
        <% end %>
    </div>
    
            <section>
        <div id="flash">
          <% flash.each do |key, value| %>
        <div class="flash <%= key %>"><%= value %></div>
        <% end %>
    </div>
    </section>
        </div>
    </main>
    
        <footer>
            <p>Created by Sahiti Meduri, Marwa Alqatari and Julia Poliakova for ITSC 3155 - 102 </p>
        </footer>
    
</body>

</html>